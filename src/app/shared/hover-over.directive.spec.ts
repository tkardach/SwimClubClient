import { HoverOverDirective } from './hover-over.directive';

describe('HoverOverDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverOverDirective();
    expect(directive).toBeTruthy();
  });
});
